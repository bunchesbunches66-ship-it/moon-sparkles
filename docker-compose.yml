version: '3.8'
services:
  nyarch-local:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NYARCH_URL=${NYARCH_URL}
      - HOST=0.0.0.0
    volumes:
      - ./data:/app/data
    restart: unless-stopped
